name: Keep QUWWAA awake
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -sS -m 30 -o /dev/null -w "%{http_code}\n" https://quwwaa.com/health || true
