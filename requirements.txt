# QUWWAA backend. The core uses only the Python standard library; pywebpush
# (and its cryptography dependency) is needed to SEND Web Push notifications.
# Render installs these via the build command (pip install -r requirements.txt).
pywebpush>=1.14.0
